# Admin AI scan (OCR) menu

**Last updated:** 2026-04-16 — **`OcrRequest.initiated_by`**: user detail “recent requests” table includes **Initiator** (`admin.ocr_requests.show.initiated_by` / `initiated_by_same_as_billed`); **`@recent_ocr_requests`** uses **`includes(:initiated_by)`**. (Earlier: 2026-04-06 — user detail show Turbo Frame main — **mega #235**; index Turbo Frame — **mega #233**; bulk **`ocr_requests_index_refresh`** — **mega #192**)

**Route:** `admin_ocr_requests_path`  
**Dashboard tile:** `admin.dashboard.critical_features.ocr_requests` (title / description) — **10 locales** — quick action uses **`data-turbo-frame="_top"`** (already in **`_dashboard_main`**).  
**Index (`GET /admin/ocr-requests`):** **`OCR_REQUESTS_INDEX_MAIN_FRAME`** = **`admin_ocr_requests_index_main`**. Page title + back-to-dashboard + **`admin_ocr_requests_index_flash`** outside the frame; **`admin_ocr_requests_index_main_wrap`** + **`_index_main`** inside. Layoutless **`ocr_requests_index_frame`** when **`Turbo-Frame`** matches. **`POST …/ocr-requests/bulk`** **`.turbo_stream`** → **`ocr_requests_index_refresh`** (unchanged targets). Export, per-user **view**, **clear search**, and show **back to list** use **`data-turbo-frame="_top"`** so CSV and user detail are not trapped in the index frame.  
**Show (`GET /admin/ocr-requests/:user_id`):** Page **`h1`** + **back to list** + **`admin_ocr_user_show_flash`** stay **outside** **`turbo-frame`** **`admin_ocr_requests_show_main`**; **`_ocr_requests_show_main`** (summary cards, actions, recent requests/denials, usage history) inside. **`OCR_REQUESTS_SHOW_MAIN_FRAME`** = **`admin_ocr_requests_show_main`**. **`Turbo-Frame`** match → layoutless **`ocr_requests_show_frame`**. **`ocr_user_refresh`** / **`ocr_user_flash_alert`** (**mega #170**) still target **`admin_ocr_user_show_flash`**, **`admin_ocr_user_summary_cards`**, **`admin_ocr_user_actions_panel`**, **`admin_ocr_user_usage_history`**. Index row **view** + show **back to list** remain **`data-turbo-frame="_top"`** (full navigation). **`admin.ocr_requests.show.*`** copy (**10 locales**).  
**Pages:** `admin/ocr_requests/index` + `show` use **`admin.ocr_requests.index.*`** / **`admin.ocr_requests.show.*`**; controller flashes and CSV headers use **`admin.ocr_requests.flash.*`** / **`admin.ocr_requests.export.*`** (all **10 locales**). Avoid raw `>` in copy rendered in ERB (HTML-escaping); use “over 80%” style wording for the near-limit stat.  
**Tests:** `spec/requests/admin/ocr_requests_spec.rb` — index + show (full page + **`Turbo-Frame: admin_ocr_requests_show_main`**), **CSV export** headers (**`I18n.t(admin.ocr_requests.export.*)`**), **adjust** (delta + reason / blank reason), **bulk** lock + **bulk reset** (translated **`bulk_reset`** notice), **toggle_lock** / **toggle_unlock** (i18n flashes), **reset_period**, **PATCH notes**, auth for index/show/export/**toggle_lock**/**reset_period**/notes.

**Related:** [admin_dashboard.md](admin_dashboard.md) (**mega #218**); [ocr_ai_gear.md](ocr_ai_gear.md) (**mega #203**); [overall/request_specs_and_gates.md](../overall/request_specs_and_gates.md) (**`ocr_requests_spec`**, **mega #233** + **mega #235**).
