# GuildSync: allow uploads up to 12MB (gear screenshots, avatars, guild logos, etc.)
# Without this, Nginx returns 413 Request Entity Too Large and the request never reaches Rails.
# Include this file in your server {} block, or add the line below directly.
client_max_body_size 12M;
