<div class="max-w-2xl mx-auto p-6">
  <div class="mb-6">
    <h1 class="text-3xl font-bold text-theme-primary mb-2"><%= t('alliances.polls.new.title') %></h1>
    <p class="text-theme-secondary"><%= @alliance.name %></p>
  </div>

  <%= form_with model: [ @alliance, @poll ], local: true, class: "bg-theme-secondary rounded-lg border border-theme-primary p-6 space-y-6" do |f| %>
    <% if @poll.errors.any? %>
      <div class="bg-red-900/20 border border-red-500 rounded-lg p-4">
        <ul class="list-disc list-inside text-red-400">
          <% @poll.errors.full_messages.each do |msg| %><li><%= msg %></li><% end %>
        </ul>
      </div>
    <% end %>

    <div>
      <%= f.label :title, t('alliances.polls.form.title'), class: "block text-sm font-medium text-theme-primary mb-2" %>
      <%= f.text_field :title, required: true, class: "w-full px-4 py-2 bg-theme-primary border border-theme-secondary rounded-lg text-theme-primary" %>
    </div>

    <div>
      <%= f.label :description, t('alliances.polls.form.description'), class: "block text-sm font-medium text-theme-primary mb-2" %>
      <%= f.text_area :description, rows: 3, class: "w-full px-4 py-2 bg-theme-primary border border-theme-secondary rounded-lg text-theme-primary" %>
    </div>

    <div>
      <%= f.label :deadline, t('alliances.polls.form.deadline'), class: "block text-sm font-medium text-theme-primary mb-2" %>
      <%= f.datetime_local_field :deadline, required: true, min: Time.current.strftime('%Y-%m-%dT%H:%M'),
          class: "w-full px-4 py-2 bg-theme-primary border border-theme-secondary rounded-lg text-theme-primary date-input-white-calendar" %>
    </div>

    <div class="flex items-center gap-3">
      <%= f.check_box :anonymous, class: "w-4 h-4 text-theme-accent bg-theme-primary border-theme-secondary rounded" %>
      <%= f.label :anonymous, t('alliances.polls.form.anonymous'), class: "text-sm font-medium text-theme-primary cursor-pointer" %>
    </div>

    <div class="flex gap-4">
      <%= f.submit t('alliances.polls.form.create_button'), class: "px-6 py-3 bg-theme-accent text-white rounded-lg hover:bg-theme-accent-hover transition-colors font-semibold cursor-pointer" %>
      <%= link_to t('common.buttons.cancel'), alliance_alliance_polls_path(@alliance), class: "px-6 py-3 bg-theme-secondary text-theme-primary rounded-lg hover:bg-theme-primary transition-colors font-semibold" %>
    </div>
  <% end %>

  <style>.date-input-white-calendar::-webkit-calendar-picker-indicator { filter: invert(1); cursor: pointer; }</style>
</div>
