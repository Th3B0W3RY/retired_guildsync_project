<div class="space-y-6">
  <div>
    <h1 class="text-2xl font-bold text-theme-primary mb-2 sm:text-4xl"><%= t('leaderboard.index.title') %></h1>
    <p class="text-theme-secondary text-base sm:text-lg"><%= t('leaderboard.index.subtitle') %></p>
  </div>

  <% if @leaderboard_data.any? %>
    <div class="bg-theme-card rounded-lg border border-theme-primary overflow-hidden">
      <div class="-mx-1 overflow-x-auto sm:mx-0">
        <table class="w-full min-w-[520px] text-sm sm:text-base">
          <thead class="bg-theme-secondary border-b border-theme-primary">
            <tr>
              <th class="px-3 py-3 text-left text-xs font-semibold text-theme-primary sm:px-6 sm:py-4 sm:text-sm"><%= t('leaderboard.index.rank') %></th>
              <th class="px-3 py-3 text-left text-xs font-semibold text-theme-primary sm:px-6 sm:py-4 sm:text-sm"><%= t('leaderboard.index.discord_username') %></th>
              <th class="px-3 py-3 text-left text-xs font-semibold text-theme-primary sm:px-6 sm:py-4 sm:text-sm"><%= t('leaderboard.index.discord_server') %></th>
              <th class="px-3 py-3 text-right text-xs font-semibold text-theme-primary sm:px-6 sm:py-4 sm:text-sm"><%= t('leaderboard.index.score') %></th>
            </tr>
          </thead>
          <tbody class="divide-y divide-theme-primary">
            <% @leaderboard_data.each_with_index do |entry, index| %>
              <tr class="hover:bg-theme-secondary transition-colors">
                <td class="px-3 py-3 text-theme-primary font-semibold sm:px-6 sm:py-4">
                  <% if index == 0 %>
                    <span class="text-yellow-500">🥇</span>
                  <% elsif index == 1 %>
                    <span class="text-gray-400">🥈</span>
                  <% elsif index == 2 %>
                    <span class="text-orange-600">🥉</span>
                  <% else %>
                    #<%= index + 1 %>
                  <% end %>
                </td>
                <td class="px-3 py-3 text-theme-primary sm:px-6 sm:py-4">
                  <%= clean_discord_username(entry[:discord_username]) %>
                </td>
                <td class="px-3 py-3 text-theme-secondary sm:px-6 sm:py-4">
                  <%= entry[:discord_server_name] %>
                </td>
                <td class="px-3 py-3 text-right text-theme-primary font-semibold sm:px-6 sm:py-4">
                  <%= entry[:score] %>
                </td>
              </tr>
            <% end %>
          </tbody>
        </table>
      </div>
    </div>
  <% else %>
    <div class="p-8 bg-theme-card rounded-lg border border-theme-primary text-center">
      <p class="text-theme-secondary mb-4"><%= t('leaderboard.index.no_data') %></p>
      <p class="text-theme-secondary text-sm"><%= t('leaderboard.index.no_data_hint') %></p>
    </div>
  <% end %>
</div>
